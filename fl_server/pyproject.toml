[project]
name = "fl-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anonymous", email = "anonymous@review.placeholder" }
]
requires-python = ">=3.13"
dependencies = [
    "flwr>=1.10.0",
]

[project.scripts]
fl-server = "fl_server:main"

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
