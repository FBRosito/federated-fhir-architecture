[project]
name = "ai-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "FBRosito", email = "fernando.rosito@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
ai-client = "ai_client:main"

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
